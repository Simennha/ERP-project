# pnpm configuration
# Auto-install peer dependencies (NestJS/Next ecosystems rely on this).
auto-install-peers=true
# Keep node_modules strict but allow common build tools to be hoisted.
strict-peer-dependencies=false
# Allow build scripts for native deps (e.g. bcrypt, prisma) to run.
enable-pre-post-scripts=true
